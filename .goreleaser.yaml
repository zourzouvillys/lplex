version: 2

before:
  hooks:
    - go generate ./pgn/...

builds:
  - id: lplex
    main: ./cmd/lplex
    binary: lplex
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  - id: lplexdump
    main: ./cmd/lplexdump
    binary: lplexdump
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  - id: lplex-cloud
    main: ./cmd/lplex-cloud
    binary: lplex-cloud
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: lplex-server
    ids:
      - lplex
    name_template: "lplex_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: lplexdump-client
    ids:
      - lplexdump
    name_template: "lplexdump_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: lplex-cloud-archive
    ids:
      - lplex-cloud
    name_template: "lplex-cloud_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

nfpms:
  - id: lplex-deb
    package_name: lplex
    ids:
      - lplex
      - lplexdump
    vendor: Theo Zourzouvillys
    homepage: https://github.com/sixfathoms/lplex
    maintainer: Theo Zourzouvillys
    description: CAN bus HTTP bridge for NMEA 2000
    license: MIT
    formats:
      - deb
    contents:
      - src: packaging/lplex.service
        dst: /lib/systemd/system/lplex.service
      - src: packaging/lplex.default
        dst: /etc/default/lplex
        type: config
    scripts:
      postinstall: packaging/postinstall.sh

brews:
  - ids:
      - lplexdump-client
    name: lplexdump
    repository:
      owner: sixfathoms
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/sixfathoms/lplex
    description: NMEA 2000 CAN bus streaming client
    license: MIT
    install: |
      bin.install "lplexdump"

dockers_v2:
  - ids:
      - lplex
    images:
      - "ghcr.io/sixfathoms/lplex"
    tags:
      - "{{ .Version }}"
      - latest
    platforms:
      - linux/amd64
      - linux/arm64
    dockerfile: Dockerfile

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
